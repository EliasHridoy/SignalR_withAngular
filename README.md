# SignalR Demo Using .Net & Angular
+ .Net 6
+ Angular
 > [!NOTE]
Here SignalR is connected to the API and calling the API in every x time interval.
For Calling API method in a time interval we used **Thread.Timer** & **System.Timer.Timer**
